#!/usr/bin/env ansible-playbook
---
- name: SatTrackClient - deploy as a service
  hosts: rotrpi
  become: True
  become_user: root
  become_method: sudo

  vars_files:
    - vars/rotrpi.yml

  roles:
    - { role: deploy-app }
